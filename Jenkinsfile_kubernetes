pipeline {
    agent any
    stages {
        stage ('build') {
            agent {
                docker {
                    image 'node:14'
                }
            }
            steps {
                echo "Build Nodejs"
                sh 'docker build -t nodejs-demo:1 .'
            }
        }
    }
}
