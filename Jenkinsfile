pipeline {
    agent any
    stages {
        stage ('build') {
            steps {
                echo "Build App"
                echo "Test Trigger"
                sleep (5)
            }
        }
    }
}
